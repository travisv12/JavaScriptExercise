// Get the button element
    var buttonElement = document.getElementById('haha');

    // Attach a click event listener to the button
    buttonElement.addEventListener('click', function() {
      alert('Button Clicked');
    });